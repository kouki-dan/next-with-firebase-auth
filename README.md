# Next.js sample app with Firebase Authentication

## Serve on local

Setup credentials before run
https://firebase.google.com/docs/functions/local-emulator#set_up_admin_credentials_optional

```bash
npm install
npm run serve
```
